apiVersion: v1
kind: Service
metadata:
  name: flavor
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "flavor"
    port: 8080
  selector:
    app: flavor
