apiVersion: v1
kind: Service
metadata:
  name: emile
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "emile"
    port: 8080
  selector:
    app: emile
