apiVersion: v1
kind: Service
metadata:
  name: each
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "each"
    port: 8080
  selector:
    app: each
