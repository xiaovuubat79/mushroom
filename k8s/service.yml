apiVersion: v1
kind: Service
metadata:
  name: color
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "color"
    port: 8080
  selector:
    app: color
